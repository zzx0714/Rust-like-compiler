根据实验文档（第5页至第16页）的内容，以下是为您整理的完整、详尽的语法产生式规则。由于文档中部分产生式采用了递进式描述（如“拓展1.1”），下文已按照逻辑顺序将所有细节完整还原：

# 类Rust语法规则详细手册

### 1. 程序与函数结构 (1.x)

* **1.1 基础程序**
* 
`<Program> -> <声明串>` 

* `<声明串> -> 空 | [cite_start]<声明> <声明串>` 

* 
`<声明> -> <函数声明>` 

* 
`<函数声明> -> <函数头声明> <语句块>` 

* 
`<函数头声明> -> fn <ID> '(' <形参列表> ')'` 

* 
`<语句块> -> '{' <语句串> '}'` 




* **1.4 函数输入**
* `<形参列表> -> 空 | <形参> | [cite_start]<形参> ',' <形参列表>` 


* 
`<形参> -> <变量属性> <ID> ':' <类型>` 




* **1.5 函数输出**
* 
`<函数头声明> -> fn <ID> '(' <形参列表> ')' '->' <类型>` 


* 
`<返回语句> -> return <表达式> ';'` 





### 2. 变量声明与赋值 (2.x)

* **2.0 变量声明**
* 
`<变量声明> -> <变量属性> <ID>` 


* 
`<变量声明> -> <变量属性> <ID> ':' <类型>` 




* **2.1 变量声明语句**
* 
`<变量声明语句> -> let <变量声明> ';'` 




* **2.2 赋值语句**
* 
`<赋值语句> -> <左值> '=' <表达式> ';'` 




* **2.3 变量声明赋值语句**
* 
`<变量声明赋值语句> -> let <变量声明> '=' <表达式> ';'` 





### 3. 表达式系统 (3.x)

* **3.1 基础表达式**
* 
`<表达式> -> <加减表达式>` 


* 
`<加减表达式> -> <项>` 


* 
`<项> -> <因子>` 


* `<因子> -> <NUM> | <左值> | [cite_start]'(' <表达式> ')'` 




* **3.2 比较运算**
* 
`<表达式> -> <表达式> <比较运算符> <加减表达式>` 


* `<比较运算符> -> '<' | '<=' | '>' | '>=' | '==' | [cite_start]'!='` 




* **3.3 加减运算**
* 
`<加减表达式> -> <加减表达式> <加减运算符> <项>` 


* `<加减运算符> -> '+' | [cite_start]'-'` 




* **3.4 乘除运算**
* 
`<项> -> <项> <乘除运算符> <因子>` 


* `<乘除运算符> -> '*' | [cite_start]'/'` 




* **3.5 函数调用**
* 
`<因子> -> <ID> '(' <实参列表> ')'` 


* `<实参列表> -> 空 | <表达式> | [cite_start]<表达式> ',' <实参列表>` 





### 4. 选择结构 (4.x)

* **4.1-4.3 If-Else**
* 
`<if语句> -> if <表达式> <语句块> <else部分>` 


* 
`<else部分> -> 空` 


* 
`<else部分> -> else <语句块>` 


* 
`<else部分> -> else if <表达式> <语句块> <else部分>` 





### 5. 循环结构 (5.x)

* **5.1-5.3 循环语句**
* `<循环语句> -> <while语句> | <for语句> | [cite_start]<loop语句>` 


* 
`<while语句> -> while <表达式> <语句块>` 


* 
`<for语句> -> for <变量声明> in <可迭代结构> <语句块>` 


* 
`<可迭代结构> -> <表达式> '..' <表达式>` 


* 
`<loop语句> -> loop <语句块>` 




* **5.4 中断语句**
* `<语句> -> break ';' [cite_start]| continue ';'` 





### 6. 引用与借用 (6.x)

* **6.1-6.3 引用类型**
* `<类型> -> '&' <类型> | [cite_start]'&' mut <类型>` 


* `<可取引用> -> '&' <可取引用> | [cite_start]'&' mut <可取引用>` 




* **6.4 解引用**
* 
`<左值> -> '*' <左值>` 





### 7. 函数表达式块与选择/循环表达式 (7.x)

* **7.1-7.2 表达式块**
* 
`<函数表达式语句块> -> '{' <函数表达式语句串> '}'` 


* `<函数表达式语句串> -> <表达式> | [cite_start]<语句> <函数表达式语句串>` 


* 
`<可取元素> -> <函数表达式语句块>` 




* **7.3-7.4 选择/循环表达式**
* 
`<选择表达式> -> if <表达式> <函数表达式语句块> else <函数表达式语句块>` 


* `<可取元素> -> <选择表达式> | [cite_start]<loop语句>` 


* 
`<语句> -> break <表达式> ';'` 





### 8. 数组 (8.x)

* **8.1 数组类型**
* `<类型> -> '[' <类型> ';' [cite_start]<NUM> ']'` 




* **8.2 数组表达式**
* 
`<可取元素> -> '[' <数组元素列表> ']'` 


* `<数组元素列表> -> 空 | <表达式> | [cite_start]<表达式> ',' <数组元素列表>` 




* **8.3 数组访问**
* 
`<可取元素> -> <可取元素> '[' <表达式> ']'` 





### 9. 元组 (9.x)

* **9.1 元组类型**
* 
`<类型> -> '(' <元组类型内部> ')'` 


* `<元组类型内部> -> 空 | [cite_start]<类型> ',' <类型列表>` 


* `<类型列表> -> 空 | <类型> | [cite_start]<类型> ',' <类型列表>` 




* **9.2 元组表达式**
* `<元组赋值内部> -> 空 | [cite_start]<表达式> ',' <元组元素列表>` 


* `<元组元素列表> -> 空 | <表达式> | [cite_start]<表达式> ',' <元组元素列表>` 




* **9.3 元组元素访问**
* `<可取元素> -> <可取元素> '.' [cite_start]<NUM>` 





### 0. 基础辅助定义

* **0.1 变量属性**：`<变量属性> -> mut | [cite_start]空` 


* 
**0.2 类型**：`<类型> -> i32` 


* 
**0.3 左值**：`<左值> -> <可取引用>` 


* **可取引用/元素**：
* 
`<可取引用> -> <可取元素>` 


* 
`<可取元素> -> <ID>`